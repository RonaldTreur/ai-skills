# Add Feature Provenance

This skill was created from local workflow discussion about existing-project
feature requests.

The adopted boundary is deliberately small: `add-feature` classifies a
feature-shaped request, creates the smallest useful GitHub issue shape, and
routes the work to existing owners. It does not replace `start-new-project`,
`project-manager`, `implement-issue`, `testing-orchestrator`, or `code-review`.
